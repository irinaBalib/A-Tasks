namespace WeatherApp.Models
{
    public class WeatherItem
    {
        //Model with properties that are necessary for the visualization only
        //As an option - would try using property mapper to serialize from Db model
    }
}
